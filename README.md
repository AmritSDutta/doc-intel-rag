# doc-intel-rag

Lightweight Document Intelligence RAG (Retrieval-Augmented Generation) reference implementation — PDF/text ingestion → chunking → embeddings → vector store → retrieval + QA.
(Repository structure verified from the public GitHub listing.) 
GitHub

Features

Modular pipeline: ingest (document parsing & chunking), index (embeddings + DB), query (RAG/QA flows). 
GitHub

Test scaffold under tests and basic evaluation utilities under eval.

